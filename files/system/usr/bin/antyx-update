#!/usr/bin/env bash
set -euo pipefail

echo "Updating Antyx-OS."

if command -v ujust >/dev/null 2>&1; then
    ujust update
else
    sudo rpm-ostree upgrade
    flatpak update -y
fi

echo
echo "Done. Restart the system if a new system image was installed."
